spring:
  application:
    name: avclient

realRequest: false

server:
  port: 9091

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - prometheus
  health:
    show-details: always